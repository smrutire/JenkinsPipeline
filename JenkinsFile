pipeline {
    agent any

    stages {
        stage('clone repo') {
            steps {
                    echo"git clone
                    https://github.com/frescoplaylab/JenkinsPipeline.git
                    
                    echo "pulled the code"
                    """)
        }
    }
        }
    }
}
